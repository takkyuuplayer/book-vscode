overwrite: true
schema: "schema.graphql"
documents: null
generates:
  src/server/generated/graphql.ts:
    plugins:
      - add: "/* eslint-disable */"
      - "typescript"
      - "typescript-resolvers"
  ./graphql.schema.json:
    plugins:
      - "introspection"
